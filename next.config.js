/** @type {import('next').NextConfig} */
const nextConfig = {
    "output": "export",
    images: {
        domains: ['https://chuliying.github.io/nextjs-blog/'],
      },
}

module.exports = nextConfig
